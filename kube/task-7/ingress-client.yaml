apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: client-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /s-43/client
spec:
  rules:
  - host: hel.westeurope.cloudapp.azure.com
    http:
      paths:
      - path: /s-43/client
        backend:
          serviceName: web-client
          servicePort: 80